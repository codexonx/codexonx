import ProjectsPage from '@/modules/app/projects/projects-page';

export const metadata = {
  title: 'Codexonx App | Projects',
  description: 'Workspace şablonlarını keşfedin ve yeni projeler başlatın.',
};

export default function AppProjectsPage() {
  return <ProjectsPage />;
}
